package com.n.alian.today.data.local
/**
 * أين تقع المهمة في الجدول الزمني.
 * العلاقة "بـ اليوم" نسبية → الترحيل اليومي يشتغل بأمر SQL بسيط.
 */
enum class Bucket {
    TODAY,      // مهام اليوم
    TOMORROW,   // بكرة — تترقّى لـ TODAY عند منتصف الليل
    LATER       // لاحقاً — تبقى مكانها لحد ما تنقلها يدوياً
}
